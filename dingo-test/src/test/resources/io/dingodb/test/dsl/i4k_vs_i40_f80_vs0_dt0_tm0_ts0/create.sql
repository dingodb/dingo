create table {table} (
    id int,
    name varchar(32) not null,
    age int,
    amount double,
    address varchar(255),
    birthday date,
    createTime time,
    update_Time timestamp,
    primary key(id)
)
