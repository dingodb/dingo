CREATE TABLE {table} (
    id int,
    name varchar(64),
    age int,
    amount DOUBLE,
    address varchar(255),
    birthday DATE,
    create_time TIME,
    update_time TIMESTAMP,
    is_delete boolean,
    PRIMARY KEY (id)
)
